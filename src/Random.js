import React from "react";
import CatContainer from "./CatContainer";

export default class extends React.Component {
  render() {
    return (
      <CatContainer isButton = {true} ></CatContainer>
    );
  }
}
